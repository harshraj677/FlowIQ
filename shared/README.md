# shared

Cross-cutting TypeScript types, constants and validation schemas shared
between `mobile` and `backend` land here once real domain models exist
(Phase 2+). Kept empty in Phase 1 to avoid guessing schemas before any
business logic is defined.
