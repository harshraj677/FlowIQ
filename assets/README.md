# assets

Design references and source brand assets (approved mockups, exported
icon/logo sources) that aren't bundled into the app itself. App-bundled
runtime assets live in `mobile/assets` and `mobile/src/assets`.
